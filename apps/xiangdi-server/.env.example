# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# XiangDi Server 环境变量
# 复制本文件为 .env 并按需修改（.env 已在 .gitignore 中）
# OS 环境变量优先级高于 .env，生产部署可通过系统环境变量注入
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ─── 服务端口 ──────────────────────────────────────────────────────
PORT=3002

# ─── Banyan 后端地址（XiangDi 从此拉取应用数据）────────────────────
BANYAN_URL=http://localhost:3001
INTERNAL_API_TOKEN=__dev_internal_token__

# ─── 知识服务地址（可选，不设则使用降级策略）───────────────────────
KNOWLEDGE_URL=http://localhost:3003
# KNOWLEDGE_INTERNAL_TOKEN=

# ─── LLM Provider 配置 ─────────────────────────────────────────────
# 主 Provider：deepseek / kimi
LLM_PROVIDER=deepseek

# DeepSeek 配置
DEEPSEEK_API_KEY=sk_your_deepseek_api_key
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Kimi 配置（备选 Provider）
# KIMI_API_KEY=sk_your_kimi_api_key
# KIMI_MODEL=kimi-k2.6
# KIMI_BASE_URL=https://api.moonshot.ai/v1

# LLM 路由配置
LLM_HIGH_LATENCY_MS=60000
LLM_CONSECUTIVE_FAIL_THRESHOLD=3

# ─── XiangDi 内部认证（banyan backend 请求本服务时的 Token）────────
XIANGDI_INTERNAL_TOKEN=__dev_internal_token__

# ─── Checkpoint 持久化 ─────────────────────────────────────────────
CHECKPOINT_BACKEND=sqlite
CHECKPOINT_DB_PATH=./data/checkpoints.db

# ─── Logging ───────────────────────────────────────────────────────
LOG_LEVEL=info
NODE_ENV=development

# ─── LangSmith Tracing（可选）──────────────────────────────────────
# 启用后自动捕获 LangGraph 执行轨迹到 smith.langchain.com
# 获取 API Key: https://smith.langchain.com/settings → API Keys → Create API Key
# 类型选择: Service Key（自动化服务使用）
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=ls_your_service_key_here
# LANGSMITH_PROJECT=xiangdi
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
