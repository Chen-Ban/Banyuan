# Electron 二进制走 npmmirror 镜像下载
# pnpm 执行 electron 的 postinstall(install.js) 时会把此 config 以
# npm_config_electron_mirror 注入环境，@electron/get 直接读取该镜像。
# 注意：依赖包的 build script 仍需在 package.json 的 pnpm.onlyBuiltDependencies 中放行。
electron_mirror=https://npmmirror.com/mirrors/electron/
