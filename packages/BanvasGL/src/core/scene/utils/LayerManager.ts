import { View } from "@/core/views";

export default class LayerManager {
    static bringToFront(views:View[],view:View){

    }
    static sendToBack(views:View[],view:View){

    }
    static bringForward(views:View[],view:View) {
    }

    static sendBackward(views:View[],view:View) {
    }

    static setLayer(views:View[],view:View, layer: number) {
        
    }
}