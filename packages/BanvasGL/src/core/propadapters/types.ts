/**
 * 属性适配器类型定义（模块内部重新导出）
 *
 * 公共类型（PropertyCategory、PropertyAdapter、PropertyDescriptor、ConflictGroup）
 * 已迁移至 @/core/interfaces/IPropertyAdapter，请从那里导入。
 *
 * 本文件保留重新导出，供 propadapters 模块内部文件继续使用 './types' 路径。
 */

export type {
    PropertyCategory,
    PropertyAdapter,
    PropertyDescriptor,
    ConflictGroup,
} from '@/core/interfaces'
